# build output
dist/
# astro generated types + cache
.astro/
# deps
node_modules/
# env / secrets — never commit
.env
.env.*
# os / editor
.DS_Store
*.log
npm-debug.log*
# local deploy settings (host alias, paths)
deploy/deploy.env
